//QUE ES LA ESTRUCTURA DE DECISIÓN
La toma de decisiones en programación es similar a la toma de decisiones en la vida real.
en programacion de la estructura de decision seguira un camino distinto según la condicion.
//Declaraciones condicionales de JavaScript
Estas declaraciones permiten controlar el flujo de ejecución de un programa basándose en las condiciones conocidas solo durante el tiempo de ejecución.
if: Se utiliza para decidir si una determinada declaración o bloque de instrucciones se ejecutará o no, es decir, si una determinada condición es verdadera, entonces un bloque de instrucción se ejecuta de otro modo se ejecutarán otras instrucciones de código.
if-else: Pero qué pasa si queremos hacer algo más si la condición es falsa. Aquí viene la otra declaración. Podemos usar la instrucción else con la instrucción if para ejecutar un bloque de código cuando la condición es falsa.
if anidado: un if anidado es una sentencia if que esta dentro de otro if o else. Anidado si sentencias significa una sentencia if dentro de una sentencia if. 
